"""قرارداد یک کانال تحویل بیرونی (P1-03).

تا امروز اعلان‌ها فقط درون‌برنامه‌ای بودند، یعنی کل گردش‌کار به این وابسته بود که
تأییدکننده خودش یادش بیفتد وارد شود. پرونده‌ای که سه هفته منتظر مانده، معمولاً
منتظر «تصمیم» نیست؛ منتظر این است که کسی بفهمد نوبتش شده.

**چرا انتزاع، به‌جای وصل‌کردن مستقیم یک سرویس:** انتخاب ارائه‌دهنده یک تصمیم است،
نه یک وابستگی. کاوه‌نگار امروز، سرویس دیگری فردا، یا SMTP برای مشتری‌ای که اصلاً
پیامک نمی‌خواهد. اگر کدِ گردش‌کار مستقیم به یکی از این‌ها بچسبد، عوض‌کردنش یعنی
بازنویسی — و بدتر، یعنی هر مشتری تازه یک انشعاب.

کانال عمداً چیز کمی می‌داند: یک پیام می‌گیرد و یا می‌فرستد یا خطا می‌دهد. صف،
تلاش مجدد، ارجحیت کاربر و تصمیم «چه چیزی اصلاً بیرون برود» همگی بیرون از کانال
هستند، در services/delivery.py. یعنی افزودن یک ارائه‌دهندهٔ تازه، یک کلاس است و
نه هیچ چیز دیگر.
"""
from dataclasses import dataclass
from typing import Protocol, runtime_checkable

from app.models.enums import DeliveryChannel


class DeliveryError(Exception):
    """ارسال شکست خورد.

    `retryable` تفاوت «شبکه قطع بود» و «شمارهٔ گیرنده غلط است» را نگه می‌دارد.
    بدون این تفکیک، صف تا ابد یک شمارهٔ نامعتبر را دوباره می‌فرستد و سهمیهٔ
    ارسال را می‌سوزاند.
    """

    def __init__(self, message: str, *, retryable: bool = True) -> None:
        super().__init__(message)
        self.retryable = retryable


@dataclass(frozen=True)
class Message:
    """یک پیام آمادهٔ ارسال.

    `subject` فقط برای کانال‌هایی معنا دارد که موضوع می‌فهمند (ایمیل)؛ پیامک
    نادیده‌اش می‌گیرد. `link` جداست تا هر کانال خودش تصمیم بگیرد چطور نشانش
    بدهد — ایمیل به‌صورت دکمه، پیامک به‌صورت نشانی کوتاه در انتهای متن.
    """

    recipient: str
    subject: str
    body: str
    link: str | None = None


@runtime_checkable
class Channel(Protocol):
    """هر ارائه‌دهندهٔ ایمیل یا پیامک این سه چیز را دارد."""

    #: کدام نوع کانال است — تعیین می‌کند نشانی گیرنده از کجا خوانده شود
    kind: DeliveryChannel

    @property
    def is_configured(self) -> bool:
        """آیا تنظیماتش کامل است.

        کانالِ نیمه‌تنظیم‌شده هرگز نباید در صف بنشیند: پیام‌هایی می‌سازد که
        قرار نیست هرگز برسند، و صف را با شکست‌هایی پر می‌کند که علتشان
        پیکربندی است نه اتفاق.
        """

    def send(self, message: Message) -> None:
        """می‌فرستد، یا `DeliveryError` می‌دهد. برگشت بی‌صدا معنایش موفقیت است."""
